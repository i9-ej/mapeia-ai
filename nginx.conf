server {
    listen 80;
    server_name MapeiaAI.i9neuro.com;

    # Rotear tráfego principal para o Frontend (Next.js)
    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Aumentar timeout para requisições longas da IA (5 minutos)
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    # Rotear /api-backend para o Backend Python (FastAPI)
    location /api-backend/ {
        # Remove o /api-backend antes de passar para o backend
        rewrite ^/api-backend/(.*) /$1 break;
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}
